services:
    lite-stream-engine:
        image: alfg/nginx-rtmp
        container_name: litestream_nginx
        # sh wrapper: ensure the notify script is executable before NGINX starts.
        # This is required on Windows where bind-mount files lose the execute bit.
        entrypoint:
            [
                "/bin/sh",
                "-c",
                "chmod +x /usr/local/bin/notify_vod.sh && chmod +x /usr/local/bin/notify_start.sh && chmod 777 /tmp/hls && nginx -g 'daemon off;'",
            ]
        ports:
            - "1935:1935"
            - "8080:8080"
        volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf:ro
            - hls_data:/tmp/hls
            # Mount the VOD notifier scripts. Not :ro so the execute bit survives.
            - ./notify_vod.sh:/usr/local/bin/notify_vod.sh
            - ./notify_start.sh:/usr/local/bin/notify_start.sh
        depends_on:
            localstack:
                condition: service_healthy
        restart: unless-stopped

    postgres:
        image: postgres:15-alpine
        container_name: litestream_db
        environment:
            POSTGRES_USER: user
            POSTGRES_PASSWORD: password
            POSTGRES_DB: litestream
        ports:
            - "5432:5432"
        volumes:
            - postgres_data:/var/lib/postgresql/data
        restart: unless-stopped

    redis:
        image: redis:alpine
        container_name: litestream_cache
        ports:
            - "6379:6379"
        restart: unless-stopped

    go-backend:
        build:
            context: ./backend
            dockerfile: Dockerfile
        container_name: litestream_backend
        environment:
            DB_USER: user
            DB_PASSWORD: password
            DB_NAME: litestream
            REDIS_HOST: redis
            AWS_ENDPOINT: http://localstack:4566
            AWS_REGION: us-east-1
            AWS_ACCESS_KEY_ID: test
            AWS_SECRET_ACCESS_KEY: test
            S3_BUCKET_NAME: vod-bucket
        ports:
            - "8000:8000"
        depends_on:
            postgres:
                condition: service_started
            redis:
                condition: service_started
            localstack:
                condition: service_healthy
        restart: unless-stopped

    localstack:
        image: localstack/localstack:3.4.0
        container_name: litestream_localstack
        ports:
            - "4566:4566"
        environment:
            - SERVICES=s3,sqs
            - DEFAULT_REGION=us-east-1
            - AWS_DEFAULT_REGION=us-east-1
            - DOCKER_HOST=unix:///var/run/docker.sock
        volumes:
            - localstack_data:/var/lib/localstack
            - /var/run/docker.sock:/var/run/docker.sock
        healthcheck:
            test:
                [
                    "CMD",
                    "curl",
                    "-f",
                    "http://localhost:4566/_localstack/health",
                ]
            interval: 5s
            timeout: 5s
            retries: 10
        restart: unless-stopped

    go-worker:
        build:
            context: ./worker
            dockerfile: Dockerfile
        container_name: litestream_worker
        environment:
            AWS_ENDPOINT: http://localstack:4566
            AWS_REGION: us-east-1
            AWS_ACCESS_KEY_ID: test
            AWS_SECRET_ACCESS_KEY: test
            SQS_QUEUE_NAME: vod-queue
            S3_BUCKET_NAME: vod-bucket
        depends_on:
            localstack:
                condition: service_healthy
        volumes:
            - hls_data:/tmp/hls
        restart: unless-stopped

volumes:
    hls_data:
    postgres_data:
    localstack_data:
